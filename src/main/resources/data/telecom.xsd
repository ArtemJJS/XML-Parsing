<?xml version="1.0" encoding="UTF-8" ?>
<schema elementFormDefault="qualified" xmlns="http://www.w3.org/2001/XMLSchema"
        targetNamespace="http://anelkin.by/telecom"
        xmlns:tns="http://anelkin.by/telecom">

    <element name="tariffs">
        <complexType>
            <sequence>
                <element name="tariff" minOccurs="0" maxOccurs="unbounded" type="tns:tariff"/>
            </sequence>
        </complexType>
    </element>

    <complexType name="tariff">
        <sequence>
            <element name="payRoll" type="tns:positiveDecimal"/>
            <element name="callPrices" type="tns:callPricePlan"/>
            <element name="smsPrice" type="tns:positiveDecimal"/>
            <element name="parameters" type="tns:tariffParameters"/>
        </sequence>
        <attribute name="name" type="string" use="required"/>
        <attribute name="operatorName" type="string" use="required"/>
        <!--attributes are optional by default -->
        <attribute name="openDate" type="date" use="required"/>
        <attribute name="closeDate" type="date"/>
    </complexType>

    <complexType name="callPricePlan">
        <attribute name="insideNetwork" type="tns:positiveDecimal" use="required"/>
        <attribute name="outsideNetwork" type="tns:positiveDecimal" use="required"/>
        <attribute name="onLandLine" type="tns:positiveDecimal" use="required"/>
        <attribute name="onFavoriteNumber" type="tns:positiveDecimal" default="0"/>
    </complexType>

    <complexType name="tariffParameters">
        <attribute name="favoriteNumbersAmount" type="nonNegativeInteger" default="0"/>
        <attribute name="tarification" type="tns:tarificationInterval" default="60"/>
        <attribute name="activationPayment" type="tns:positiveDecimal" default="0"/>
    </complexType>

    <simpleType name="positiveDecimal">
        <restriction base="decimal">
            <minInclusive value="0"/>
        </restriction>
    </simpleType>

    <simpleType name="tarificationInterval">
        <restriction base="positiveInteger">
            <enumeration value="1"/>
            <enumeration value="12"/>
            <enumeration value="30"/>
            <enumeration value="60"/>
        </restriction>
    </simpleType>
</schema>